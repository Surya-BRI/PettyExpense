# Flutter app → backend API
#
# Copy the URL you want into API_BASE_URL below, then full restart (not hot reload).
#
# Prod (default):                   https://expensetracker-api.app-brisigns.com
# Dev — Android emulator:            http://10.0.2.2:8000   (NOT 127.0.0.1 — that's the phone itself)
# Dev — iOS simulator / Windows / Chrome: http://127.0.0.1:8000
# Dev — physical phone (same Wi-Fi): http://<your-PC-LAN-IP>:8000

API_BASE_URL=http://10.0.2.2:8000 
