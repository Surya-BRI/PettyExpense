fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic-settings>=2.2.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1  # passlib 1.7.4's bcrypt self-test breaks on bcrypt>=4.1's stricter 72-byte handling
sqlalchemy>=2.0.0
pyodbc>=5.0.0
boto3>=1.34.0
python-dotenv>=1.0.0
httpx>=0.27.0
pillow>=10.0.0
# OCR (default backend; falls back to stub if not installed)
paddlepaddle>=3.0.0
paddleocr>=3.0.0
pyyaml>=6.0.0

# Test-only (not needed to run the app; no separate dev-requirements file
# exists in this repo yet, so it's marked here rather than installed by default)
pytest>=8.0.0
